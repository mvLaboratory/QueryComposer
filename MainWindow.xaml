<Window x:Class="QueryComposer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QueryComposer"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Border Padding="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <TabControl Grid.Row="0" Margin="5,-5,-5,5">
                <TabItem>
                    <StackPanel>
                        <TextBlock>l1</TextBlock>
                        <TextBlock>l2</TextBlock>
                    </StackPanel>
                </TabItem>

                <TabItem>Tab2</TabItem>
            </TabControl>

            <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Name="Ok" Content="OK" />

                <Button Name="Cancel" Content="Cancel"/>

            </StackPanel>
        </Grid>
    </Border>
</Window>
